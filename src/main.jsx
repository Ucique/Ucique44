import React from "react";
import { createRoot } from "react-dom/client";
import UciqueHome from "./UciqueHome.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <UciqueHome />
  </React.StrictMode>
);
